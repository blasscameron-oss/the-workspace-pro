name = "the-workspace-pro"
pages_build_output_dir = "./_site"
compatibility_date = "2026-04-18"

# Cloudflare Pages configuration (static site)
# No [site] or [[routes]] needed – Pages handles static asset caching automatically