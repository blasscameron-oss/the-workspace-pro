# Allow Cloudflare Pages domain verification
/.well-known/* 200

# Exclude Google Search Console verification file from SPA fallback
/google803b5c2f4a1e9d87.html 200

# SPA fallback - for client-side routing (content pages now at clean URLs via deploy.yml)
/* /index.html 200
